from setuptools import setup

setup(name='distributions-irvifa',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions'],
      install_requires=['matplotlib'],
      zip_safe=False)
